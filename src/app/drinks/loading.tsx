import { Sheet } from "@/components/Sheet";

export default function DrinksLoading() {
  return (
    <Sheet title="wine & drinks" kicker="natural wine, cocktails, beer">
      <p className="empty-copy">pulling the list…</p>
    </Sheet>
  );
}
